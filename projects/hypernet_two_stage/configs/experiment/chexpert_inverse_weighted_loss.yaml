# @package _global_
defaults:
  - _base_chexpert
  - _self_

experiment_name: two-stage-chexpert-inverse-weighted-loss
tags: [chexpert, two_stage, inverse_weighted_loss]

# workflow が train split の class frequency から class_weight を解決し、両 stage が参照する。
weighting: inverse
