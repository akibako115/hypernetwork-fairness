# @package _global_
# 両 stage が共有する CheXpert の入力と実行条件。stage 固有の設定は stage1 / stage2 group が持つ。
defaults:
  - /data: chexpert
  - _self_

dataset: chexpert
weighting: none

trainer:
  max_epochs: 30

data:
  num_workers: 8
  pin_memory: true
  persistent_workers: true
  prefetch_factor: 2
