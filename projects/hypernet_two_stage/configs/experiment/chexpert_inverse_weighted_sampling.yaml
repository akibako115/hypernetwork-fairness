# @package _global_
defaults:
  - _base_chexpert
  - _self_

experiment_name: two-stage-chexpert-inverse-weighted-sampling
tags: [chexpert, two_stage, inverse_weighted_sampling]

data:
  train_sampling: inverse_frequency
