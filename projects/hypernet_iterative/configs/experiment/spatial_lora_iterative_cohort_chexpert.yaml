# @package _global_
defaults:
  - /data: chexpert_cohort
  - /model: spatial_lora_resnet50
  - /cohort_definition: metadata_kmeans
  - /training_strategy: group_dro
  - /checkpoint_selection: global_auroc_bacc
  - /callbacks/hidden_cohort@callbacks
  - _self_

dataset: chexpert
experiment_name: spatial-lora-iterative-cohort-chexpert
weighting: inverse
trainer:
  max_epochs: ${iteration.stage_epochs}
data:
  group_assignment_path: null
model:
  warm_start_checkpoint_path: null
