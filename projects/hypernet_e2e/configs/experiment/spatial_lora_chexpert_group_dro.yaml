# @package _global_
# class weight は CheXpert 系列の既定（inverse）を受け、GroupDROTaskLoss.class_weight に解決される。
# hypernet_iterative の cohort GroupDRO も同じ weighting で回しているので、条件が揃う。
defaults:
  - spatial_lora_chexpert
  - override /data: chexpert_demographic_groups
  - override /training_strategy: group_dro
  - _self_

experiment_name: spatial-lora-chexpert-group-dro
