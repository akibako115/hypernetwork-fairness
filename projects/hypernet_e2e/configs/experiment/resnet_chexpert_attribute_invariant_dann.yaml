# @package _global_
defaults:
  - /model: resnet50_attribute_invariant_dann
  - _base_chexpert
  - _self_

experiment_name: resnet-chexpert-attribute-invariant-dann
tags: [attribute_invariant_dann]

attribute_invariance:
  enabled: true
